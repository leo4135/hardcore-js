# Ревью Day 1 — Block A

## ❌ КРИТИЧЕСКИЕ ОШИБКИ МЫШЛЕНИЯ

### 1. Фрагмент 2 (prediction.js) — НЕВЕРНЫЙ ОТВЕТ
**Твой ответ:** "Выведется 1, 2"
**Правильный ответ:** 2, 1

**Проблема:** Неправильно посчитал вызовы:
- `counter1.increment()` → count = 1
- `counter1.increment()` → count = 2
- `counter2.increment()` → count = 1

**Глубже:** Объяснение "на момент вызова функции будет ссылаться на count" — неточное. Каждый вызов `createCounter()` создаёт **НОВОЕ лексическое окружение** с **НОВОЙ переменной count**. Это не "ссылка на момент вызова", а **независимые замыкания**.

---

### 2. Фрагмент 5 (prediction.js) — КРИТИЧЕСКАЯ ОШИБКА
**Твой ответ:** "Выведется 2"
**Правильный ответ:** 999

**Проблема:** Не понимаешь, что объекты захватываются **по ссылке**, а не по значению.

```javascript
const stateRef = state1.getState(); // stateRef === state (та же ссылка!)
stateRef.value = 999;                // Изменяем ТОТ ЖЕ объект
console.log(state1.getValue());      // 999, не 2!
```

**Глубже:** 
- `const state = { value: 0 }` — создаётся объект в памяти
- `getState: () => state` — возвращает **ссылку** на этот объект
- `stateRef.value = 999` — изменяет объект по ссылке
- Все переменные, ссылающиеся на этот объект, видят изменения

**Твоё объяснение:** "stateRef - просто примет свойство со значением, без контекста" — **полностью неверно**. Это не копия, это ссылка!

---

### 3. Фрагмент 4 (prediction.js) — НЕТОЧНОСТЬ
**Твой ответ про повторный вызов:** "каждый раз будет пересчитывать сумму исходя из того, что x + y - статичный, а z - будет в зависимости от того, что мы передали в аргументы"

**Проблема:** `fn2` уже создан с `y = 20`. Его нельзя "вызвать с другим y". Можно только вызвать `fn2(30)` или `fn2(40)`, но `y` всегда будет 20.

**Глубже:** Не понимаешь, что замыкание "замораживает" значения из внешних scope на момент создания функции, а не на момент вызова.

---

## ⚠️ ЧАСТИЧНОЕ ПОНИМАНИЕ

### 4. Фрагмент 1 (prediction.js)
**Ответ правильный**, но объяснение неточное:
- "не имеющих своего контекста" — неточно
- Правильнее: все функции ссылаются на **одну и ту же переменную** `i` (из-за `var`)

### 5. Фрагмент 3 (prediction.js)
**Ответ правильный**, но объяснение неточное:
- "запоминает ссылку на j в момент помещения в макротаски" — неточно
- Правильнее: `let` создаёт **новую переменную** на каждой итерации, и каждая функция захватывает **свою копию**

---

## ❌ КРИТИЧЕСКИЙ ПРОБЕЛ: Higher-Order Functions

### 6. implementation.js — memoize
**Твой комментарий:** "не понимаю как она может вернуть функцию с тем же api, если у меня нет доступа к сигнатуре функции"

**Проблема:** Не понимаешь концепцию **higher-order functions** (функции высшего порядка).

**Решение:** Функция может принимать функцию и возвращать функцию. Возвращаемая функция использует `...args` для передачи аргументов:

```javascript
function memoize(fn) {
  const cache = {};
  
  return function(...args) {  // ← Это и есть "тот же API"
    const key = JSON.stringify(args);
    if (!cache[key]) {
      cache[key] = fn(...args);  // Вызываем оригинальную функцию
    }
    return cache[key];
  };
}
```

---

## ❌ КРИТИЧЕСКИЙ ПРОБЕЛ: Приватные переменные через замыкания

### 7. implementation.js — createPrivateStore
**Твой комментарий:** "понятия не имею как это сделать. Мне нужна мат часть и плавное введение в такие задачи"

**Проблема:** Не понимаешь базовый паттерн создания приватных переменных через замыкания.

**Решение:**
```javascript
function createPrivateStore() {
  const store = {};  // Приватная переменная в замыкании
  
  return {
    set: (key, value) => { store[key] = value; },
    get: (key) => store[key],
    has: (key) => key in store,
    delete: (key) => { delete store[key]; },
    clear: () => { Object.keys(store).forEach(k => delete store[k]); }
  };
}
```

---

## ⚠️ DEBUG.JS — ЧАСТИЧНОЕ ПОНИМАНИЕ

### 8. БАГ 1
**Твой ответ:** "Emitter нельзя переопределить так как выше он был через const объявлен"

**Проблема:** Неправильно понял задачу. Проблема не в переопределении, а в том, что:
1. Нужно сохранить ссылку на callback для его удаления
2. `emitter.off('click')` не работает без ссылки на callback

**Правильное решение:**
```javascript
const callback = () => {
  data.count++;
  console.log(`Component ${data.id} clicked ${data.count} times`);
};

emitter.on('click', callback);

return {
  destroy: () => {
    emitter.off('click', callback);  // Нужна ссылка на callback!
  }
};
```

### 9. БАГ 2
Правильно понял проблему с `var`, но не исправил код.

### 10. БАГ 3, 4, 5
Не ответил на вопросы.

---

## 📋 СПИСОК КОНКРЕТНЫХ ПРОБЕЛОВ

1. **Не понимает, что объекты захватываются по ссылке в замыканиях**
   - Считает, что `getState()` возвращает "копию" или "значение без контекста"
   - Не понимает, что изменение объекта по ссылке влияет на все переменные

2. **Не понимает, что каждый вызов функции создаёт новое лексическое окружение**
   - Путает "момент создания замыкания" и "момент вызова функции"
   - Не понимает, почему `createCounter()` дважды создаёт независимые счётчики

3. **Не понимает higher-order functions**
   - Не понимает, как функция может возвращать функцию
   - Не понимает, как передавать аргументы через `...args`

4. **Не понимает паттерн приватных переменных через замыкания**
   - Не понимает, как создать переменную в замыкании и вернуть методы для работы с ней

5. **Неточное понимание момента захвата переменной**
   - Путает "захват на момент создания функции" и "захват на момент вызова"
   - Не понимает, что `let` в цикле создаёт новую переменную на каждой итерации

6. **Не понимает разницу между примитивами и объектами в замыканиях**
   - Примитивы захватываются по значению (копия)
   - Объекты захватываются по ссылке (та же ссылка)

---

## ✅ ЧТО СДЕЛАНО ХОРОШО

1. Правильно понял проблему с `var` в циклах
2. Правильно предсказал поведение в фрагментах 1 и 3
3. Правильно понял проблему с `var` в БАГ 2

---

## 🎯 СЛЕДУЮЩИЙ ШАГ

Создан **block-remedial-1** с фокусом на:
1. Захват объектов по ссылке vs примитивов по значению
2. Независимые лексические окружения при каждом вызове функции
3. Higher-order functions и возврат функций
4. Паттерн приватных переменных через замыкания

